import styled from "styled-components";

const NotFoundStyled = styled.div`
  .not-found {
    &__title {
      font-size: 2rem;
    }
    &__message {
      font-style: italic;
    }
  }
`;

export default NotFoundStyled;
